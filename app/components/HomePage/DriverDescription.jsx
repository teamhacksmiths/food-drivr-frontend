import React from 'react';

const p1 = `There are people who would rather see their extra food do
  some good instead of going to waste! We accept donations from business such
  as catering companies & facilites, restaurants and forcery stores,
  as well as donations from individuals.`;

const p2 = `Food Drivr provides an easy way for both drivers
  and donors to help put an end to hunger in their communities.`;

const DriverDescription = () => (
  <div>
    <p>{p1}</p>
    <p>{p2}</p>
  </div>
);

export default DriverDescription;
